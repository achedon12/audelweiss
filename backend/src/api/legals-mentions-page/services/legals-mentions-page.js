'use strict';

/**
 * legals-mentions-page service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::legals-mentions-page.legals-mentions-page');
