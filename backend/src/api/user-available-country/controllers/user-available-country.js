'use strict';

/**
 * user-available-country controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::user-available-country.user-available-country');
