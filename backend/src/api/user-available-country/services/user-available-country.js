'use strict';

/**
 * user-available-country service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::user-available-country.user-available-country');
