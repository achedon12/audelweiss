'use strict';

/**
 * user-available-country router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::user-available-country.user-available-country');
