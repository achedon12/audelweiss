'use strict';

/**
 * legals-mentions-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::legals-mentions-page.legals-mentions-page');
